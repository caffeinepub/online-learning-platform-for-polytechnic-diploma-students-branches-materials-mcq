## Online Learning Platform for Polytechnic Diploma Students (Branches, Materials, MCQs, Search, Admin)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
